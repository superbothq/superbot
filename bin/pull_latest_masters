#!/usr/bin/env sh
set -e

for component in superbot-teleport superbot-selenium-webdriver superbot-cloud superbot-runner superbot-runner-side superbot-record superbot-record-chrome; do
  cd ../$component
    pwd
    git checkout master
    git pull --rebase origin master
  cd ../superbot
done
